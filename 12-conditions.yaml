- name: user exists or not 
  hosts: localhost
  become: yes
  tasks:
   - name: check if user exists
     ansible.builtin.user: id expense

   - name: say hello
     ansible.builtin.debus:
      msg: "hello world"  
